{% set name = "ale" %}
{% set version = "0.9.0" %}
package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/DOI-USGS/{{ name }}/archive/{{ version }}.tar.gz
  sha256: 6fa10ef9fb5d85d3627d91e13e38a49308adf2d02726b09bf0560986c0d3a91e

build:
  number: 0
  entry_points:
    - isd_generate = ale.isd_generate:main
  skip: true  # [py2k]

requirements:
  build:
    - {{ compiler('cxx') }}
    - cmake >=3.15
    - make
    - eigen
  host:
    - python
    - nlohmann_json
    - pip
    - setuptools
  run:
    - networkx
    - {{ pin_compatible('nlohmann_json', min_pin='x.x', max_pin='x.x') }}
    - numpy
    - pvl >=1.0
    - python
    - python-dateutil
    - pytz
    - scipy >=1.4.0
    - spiceypy >=2.3.0
    - pyyaml
    - setuptools

test:
  requires:
    - setuptools
  imports:
    - ale
  commands:
    - test -e $PREFIX/lib/libale${SHLIB_EXT}  # [unix]
    - if not exist %LIBRARY_BIN%\ale.dll exit 1  # [win]
    - if not exist %LIBRARY_LIB%\ale.lib exit 1  # [win]
    - isd_generate --version

about:
  home: https://github.com/USGS-Astrogeology/ale
  license: CC0-1.0
  license_file: LICENSE.md
  summary: Abstraction Library for Ephemerides (ALE) for extracting and processing planetary sensor ephemeris data.

extra:
  recipe-maintainers:
    - jlaura
    - jessemapel
    - andrewannex
    - acpaquette
